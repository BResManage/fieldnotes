# Fieldnotes

A personal research dashboard. Public application code only; workspace data is stored privately in the Google Drive account chosen by the user.

Sign in and Drive storage can use different Google accounts. Configure the public Google OAuth client ID in config.js and enable GitHub Pages from the main branch, root folder.

The preview contains fictional sample data and resets on reload. No AI API is called by this website.
